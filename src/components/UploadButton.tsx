'use client';

import { useState } from 'react';

const UploadButton = () => {
  const [isOpen, setIsOpen] = useState<boolean>(false);

  return (
    <div>
      <p>test</p>
    </div>
  );
};

export default UploadButton;
